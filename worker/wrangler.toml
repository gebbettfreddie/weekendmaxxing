name = "weekendmaxxing-proxy"
main = "src/index.ts"
compatibility_date = "2026-06-01"

# Create the namespace with:  npx wrangler kv namespace create CACHE
# then paste the returned id below before `wrangler deploy`.
[[kv_namespaces]]
binding = "CACHE"
id = "REPLACE_WITH_KV_ID"
